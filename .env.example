# Copy to .env in project root (used by docker-compose and the GUI)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=zoo_db
DB_USER=postgres
DB_PASSWORD=mysecretpassword

POSTGRES_DB=zoo_db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=mysecretpassword
